export enum Route {
  Home = "/",
  Expenses = "/expenses",
  Expense = "/expenses/[id]",
  Users = "/users",
  User = "/users/[id]",
  Login = "/login",
  ApiExpenses = "/api/expenses",
  ApiUsers = "/api/users",
  ApiCompanies = "/api/companies",
}
