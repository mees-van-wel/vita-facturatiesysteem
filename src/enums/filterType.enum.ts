export const enum FilterType {
  Number = "NUMBER",
  String = "STRING",
  Date = "DATE",
  Decimal = "DECIMAL",
  Status = "STATUS",
}

export const enum FilterExtraType {
  Equals = "equals",
  Greater = "gte",
  Lesser = "lte",
  Between = "BETWEEN",
}
